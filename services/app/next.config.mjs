/** @type {import('next').NextConfig} */
const nextConfig = {
	distDir: 'build',
	output: 'export',
	// assetPrefix: "/app",
	// publicRuntimeConfig: {
	// 	basePath: "/app",
	// },
	// basePath: "/test",
	// assetPrefix: "/tmp"
};

export default nextConfig;
