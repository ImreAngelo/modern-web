services:
  app:
    build: 
      context: services/app
      target: standalone
    image: build-app:latest
  proxy:
    build:
      context: services/nginx
      target: development
    depends_on:
      - app
    ports:
      - 80:80
      - 443:443
      - 443:443/udp